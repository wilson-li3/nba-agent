[pytest]
# The repo has no installable package, so make the project root importable.
# Without this, the bare `pytest` binary fails with "No module named 'app'"
# while `python -m pytest` works — because only the latter puts CWD on sys.path.
pythonpath = .
testpaths = tests
